import { input } from "./12-input";
const testInput = `OOOOO
OXOXO
OOOOO
OXOXO
OOOOO`;

const part1 = (input: string) => {
  const map = input.split("\n").map((line) => line.split(""));
  return result;
};

part1(testInput); //?
